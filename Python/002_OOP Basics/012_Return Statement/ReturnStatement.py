def cube(number):

    """
    result = number * number * number
    return result
    """

    return number * number * number # This is valid as well as more compact. Return tells the compiler that the execution of the functions code is done
    
    print("Hi. I am a lost cause") # This code will never be executed as the compiler has already stopped the function code execution

"""
answer = cube(5.0)
print(answer)
""" 

print(cube(5.0)) # This is valid as well as more compact